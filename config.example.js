const CONFIG = {
    API_URL: 'http://127.0.0.1:8000/api/v1',
    BUSINESS_ID: 'tu-business-id-uuid', // Reemplazar con el UUID del negocio correspondiente en la base de datos
    API_KEY: 'tu-api-key-de-acceso', // Reemplazar con la clave de API configurada en el servidor (ej: X-API-KEY)
    PUBLIC_ONLY: true // Cambiar a false si querés listar productos privados durante pruebas
};
